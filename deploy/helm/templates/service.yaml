apiVersion: v1
kind: Service
metadata:
  name: pcd-migration
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: pcd-migration
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
